# Copy to .env and fill in values before running docker-compose

# Required: PostgreSQL password
POSTGRES_PASSWORD=changeme

# Optional: if set, implants must send this value in X-Wraith-Token
# Leave blank to allow direct implant connections (no redirector)
REDIRECTOR_TOKEN=
